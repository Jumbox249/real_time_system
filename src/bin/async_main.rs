/// async_main – superseded by the main wiki_rt_monitor binary.
/// Run `cargo run --release -- --mock` instead.
fn main() {
    eprintln!("This binary is deprecated. Run:");
    eprintln!("  cargo run --release -- --mock");
}
